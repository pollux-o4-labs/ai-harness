<!--
폼 대상: docs/history/<B|G>-<slug>.md
파일명 접두사가 판정이다: B = 하지 말 것, G = 대신 할 것.
예산(셋 다 상한): 100줄 · 산문 한 줄 80자·한 문장 · BLUF 한 줄 100자.
  표 행·코드펜스 내부는 줄 길이에서 면제한다 — 쪼개면 깨진다.
  자동생성 블록(BLUF-INDEX)도 면제한다 — 저자가 손댈 수 없는 몫을 예산에
  넣으면 지킬 방법이 없다. 줄이려면 원본 문서의 BLUF를 쳐라(그쪽이 정본).
  이 수치의 정본은 이 파일이고 scripts/check_doc_form.py가 파싱해 쓴다.
  BLUF는 이 문서가 "무엇에 관한 것인지"만 알려주는 라우팅 이정표다(규칙 08 제1조).
  결론·실측 수치·논증은 본문에 담는다 — 그래야 인덱스 롤업이 안 비대해지고
  BLUF가 stale해지지 않는다(다른 유형과 같은 100자).
아래 뼈대를 복붙하고 <> 안을 채운 뒤, 이 HTML 주석과 힌트 주석은 지운다.
-->

> **BLUF:** <실측 한 문장 — 무엇이 문제였고 무엇을 대신 하라는 결론인지.
> 재현 가능한 핵심 수치·사례가 있으면 압축해 담는다.>

## B (하지 말 것)

<!-- 무엇을 했고 왜 문제였는지 + 재현 가능한 근거(파일:라인, 실측 수치,
     대조 실험). 추측이 아니라 실측이어야 한다. 파일명이 G-*면 이 섹션은
     비교 대상 없이 G만 있어도 된다. -->

## G (대신 할 것)

<!-- 대신 해야 할 것과 그 이유 — B의 원인을 구조적으로 막는 조치. 이미 다른
     문서가 이 패턴을 시범했다면 그 문서를 링크한다(재서술 금지, 규칙 08 제2조). -->

## 승격

<!-- 이 사례가 어느 규칙 몇 조/ADR로, 언제 승격됐는지. 아직이면 "미승격"이라고
     쓴다 — 이 섹션이 "규칙을 볼 때 왜 이렇게 정해졌는가"의 역참조 지점이다. -->
