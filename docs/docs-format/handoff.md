> **BLUF:** docs/handoff/*.md의 줄 예산 폼 — 뼈대 없이 예산만 강제한다.

<!--
폼 대상: docs/handoff/*.md
예산(셋 다 상한): 150줄 · 산문 한 줄 80자·한 문장 · BLUF 100자.
  표 행·코드펜스 내부는 줄 길이에서 면제한다 — 쪼개면 깨진다.
  자동생성 블록(BLUF-INDEX)도 면제한다 — 저자가 손댈 수 없는 몫이다.
  이 수치의 정본은 이 파일이고 scripts/check_doc_form.py가 파싱해 쓴다.
  다른 곳에 옮겨 적지 말 것 — 골격을 두 군데 두면 어긋난다.
뼈대는 없다 — 칸의 역할·시간축·splice 규약은 docs/handoff/README.md가 정본이다.
  여기 옮겨 적으면 정본이 둘이 된다(규칙 08 제2조).
왜 rules의 100이 아닌가: handoff는 원장이다. 함정과 질문은 겪은 만큼 쌓이고,
  줄 예산을 못 맞춘다고 지울 수 없다 — 지우면 다음 세션이 같은 함정을 다시
  밟는다. 그게 이 문서의 존재 이유다.
정직 표기: 150은 이상적 수치가 아니라 재작성 후 실측 크기에서 왔다.
넘었을 때: 칸을 늘리지 마라(세 칸 고정). 죽은 항목을 지워라 — 해소된 함정,
  ADR로 승격된 질문, 머지된 PR은 여기 있을 이유가 없다. 그래도 안 줄면 근거를
  docs/history/로 내리고 링크만 남긴다.
-->
