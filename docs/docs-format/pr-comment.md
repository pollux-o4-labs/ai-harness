> **BLUF:** `gh pr comment`(PR 코멘트) 근거 기록의 폼 — 뼈대와 줄 예산을 함께 강제한다.

<!--
폼 대상: `gh pr comment`로 다는 리뷰 항목별 근거 코멘트(PR 본문이 아니다).
예산(둘 다 상한): 40줄 · 산문 한 줄 80자·한 문장.
  코드펜스 내부는 줄 길이·문장 규칙에서 면제한다 — 명령·출력 인용은 쪼개면
  깨진다.
  이 수치의 정본은 이 파일이고 scripts/check_pr_body.py가 파싱해 쓴다.
  다른 곳에 옮겨 적지 말 것 — 골격을 두 군데 두면 어긋난다.
왜 PR 본문 폼(scripts/check_pr_body.py의 SECTION_BUDGETS)과 다른가: 코멘트는
  섹션 골격(요약/변경/범위 밖/검증)·체크리스트 대상이 아니다 — 리뷰 항목별
  근거 기록이지 PR 본문이 아니다.
다만 내부 용어 풀이(JARGON_TERMS)는 그대로 적용한다 — 체크리스트의 은어
  검수 항목("과한 내부 은어 사용 검수했다")이 PR에 남는 글 전체에 적용되고,
  코멘트도 PR에 남는 글이기 때문이다.
뼈대: 헤더 한 줄 + `##` 범주 섹션. 그 안은 자유 형식이다.
  헤더에 차수와 대상 커밋을 적는다 — `## 리뷰 종합 — 2차 (8c8f4f7)`.
  타래가 길어지면 읽는 사람이 최신 판정과 그 대상 커밋을 못 찾는다.
  `##` 범주 단위로 `<details>`에 접는다 — 읽는 사람이 필요한 범주만 편다.
  확인 항목 근거 섹션은 접지 않는다 — 그것이 이 코멘트의 본체다.
  접어도 줄수·줄자수 예산은 그대로 산입한다(검사기가 `<details>`를 면제하지
  않는다) — 접기는 읽는 순서를 정할 뿐 양을 늘리는 면제가 아니다.
-->
