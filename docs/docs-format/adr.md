<!--
폼 대상: docs/adr/<NNNN>-<slug>.md
골격: Michael Nygard ADR 표준(제목·상태·맥락·결정·결과) + MADR의 검토한 대안.
예산(셋 다 상한): 100줄 · 산문 한 줄 80자·한 문장 · BLUF 한 줄 100자(마커 제외).
  표 행·코드펜스 내부는 줄 길이에서 면제한다 — 쪼개면 깨진다.
  자동생성 블록(BLUF-INDEX)도 면제한다 — 저자가 손댈 수 없는 몫을 예산에
  넣으면 지킬 방법이 없다. 줄이려면 원본 문서의 BLUF를 쳐라(그쪽이 정본).
  100줄을 넘으면 한 ADR에 결정이 여러 개 섞인 것이니 쪼갠다(1 레코드 = 1 결정).
  이 수치의 정본은 이 파일이고 scripts/check_doc_form.py가 파싱해 쓴다.
불변성: Accepted된 ADR은 고쳐 쓰지 않는다. 결정이 번복되면 새 ADR을 쓰고
  이쪽 상태를 Superseded by ADR NNNN으로 바꾼다.
아래 뼈대를 복붙하고 <> 안을 채운 뒤, 이 HTML 주석과 힌트 주석은 지운다.
-->

# ADR <NNNN> — <결정을 읽히게 쓴 제목>

> **BLUF:** <이 결정이 무엇에 관한 것인지 한 줄. 근거·수치는 본문에.>

- 상태: <Proposed | Accepted | Deprecated | Superseded by ADR NNNN>
- 날짜: <YYYY-MM-DD>

## 맥락

<!-- 왜 지금 이 결정을 해야 하나 — 이전 상태·제약·실측이 드러낸 문제.
     중립적 사실 위주로 쓴다. 비판·감정 없이. -->

## 검토한 대안

<!-- 최종 선택에서 탈락한 대안과 탈락 이유. 이 절이 상세할수록 "왜 A 대신
     B는 안 썼나"라는 재논의가 안 열린다. 대안이 하나도 없었으면 "없음"과
     그 이유를 적는다 — 빈 자리를 남기지 않는다. -->

## 결정

<!-- 무엇으로 정했나. "~하기로 한다" 능동태. How보다 방향과 Why. -->

## 결과 · 한계 (정직 표기)

<!-- 이 결정이 만드는 효과와, 감수하기로 한 트레이드오프·기술부채.
     **장점만 적으면 안 된다** — 아직 강제 안 되는 부분·후속 과제가 있으면
     숨기지 말고 여기 적는다. -->

## 관련

<!-- 이 결정을 낳은 실측·리뷰·history 링크, 대체/정정 관계의 다른 ADR,
     관련 규칙·스킬. -->
