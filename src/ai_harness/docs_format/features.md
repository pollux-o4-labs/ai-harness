<!--
폼 대상: docs/features/<군>.md (예: routing.md, gate.md)
저작 규범(무엇을 반드시 담아야 하는가)의 정본은 docs/features/GUIDE.md다.
  이 폼은 그 골격만 복붙 가능하게 옮긴 것이니, GUIDE.md가 바뀌면 같이
  갱신한다. 규범 내용을 여기 재서술하지 말 것.
예산(셋 다 상한): 100줄 · 산문 한 줄 80자·한 문장 · BLUF 한 줄 100자(마커 제외).
  표 행·코드펜스 내부는 줄 길이에서 면제한다 — 쪼개면 깨진다.
  자동생성 블록(BLUF-INDEX)도 면제한다 — 저자가 손댈 수 없는 몫을 예산에
  넣으면 지킬 방법이 없다. 줄이려면 원본 문서의 BLUF를 쳐라(그쪽이 정본).
  이 수치의 정본은 이 파일이고 scripts/check_doc_form.py가 파싱해 쓴다.
아래 뼈대를 복붙하고 <> 안을 채운 뒤, 이 HTML 주석과 힌트 주석은 지운다.
-->

# features/<slug> — <라벨>

> **BLUF:** <이 기능군이 무엇에 관한 것인지 한 줄.>

인덱스: [README.md](README.md) · 정본: <설계 근거 ADR·domain.md 절 링크>

<이 기능군이 하는 일 1~2문단.>
<코드가 스스로 보여주는 것(시그니처 등)은 재서술 금지 — "무엇을 왜 이렇게"
설계 의도만 적는다.>

> **검증 실측**: `<재현 커맨드>` → **<N> passed, exit 0** (<YYYY-MM-DD>,
> 감독 직접재현, <마커·인프라 조건>).
>
> 표기 — 각 항목 끝 `[✅ 파일::함수]`=검증됨 / `[⚠️ 없음]`=테스트 갭 /
> `[🔴 결함]`=현 구현이 이 불변을 깸.

---

## <코드1> · <슬라이스 라벨> — `<진입점>` · `<핵심 심볼>`

<이 슬라이스가 하는 일 1문단.>

**✔️ 작동해야**
- <불변 1>. [✅ <test_파일>::<함수> 또는 ⚠️ 없음]

**⛔ 발생하면 안 됨**
- <있으면 안 되는 상태>. [✅ 차단 <test_파일>::<함수> 또는 ⚠️ 없음]

<!-- 슬라이스마다 이 블록(##, ✔️, ⛔)을 반복한다. 코드 접두사(A1/B1/C1...)는
     README.md 기능 지도 표의 # 열과 1:1로 맞춘다 — 새 코드를 붙였으면
     README.md 표 한 줄도 같이 splice한다(미등재=잊혀짐). -->

---

## 실제 e2e 사용 케이스 (필수, GUIDE.md 규율)

<!-- 단위 불변만으로는 못 잡는 "실제로 쓸 때 조각이 안 맞물리는 갭"을 잡는다.
     사용자=AI 관점 시나리오로 최소 1개(E2E-0, E2E-1...). 각 시나리오는
     어디까지 결정적 테스트로 실증됐고 어디부터 실인프라 미실측인가를 밝힌다
     — 실인프라 E2E가 없다고 못 쓰는 절이 아니다. -->

**E2E-<N> <시나리오 이름> (실증 | 부분 실증/설계)**: <입력 → 기대 산출 경로>.
- 실증: <어느 테스트가 어디까지 증명했는지, 파일::함수>.
- 미실측: <실인프라 중 아직 안 돈 부분>.

---

## 핵심 파일·재현

- 소스: `<src/<패키지>/{모듈,...}.py>`.
- 테스트(본 문서 출처): `<test_파일>`(N)·... = 합계.
- 인접·미인용: <이 슬라이스에 닿지만 출처 밖인 테스트, 없으면 생략>.
- 재현: `<pytest 커맨드>` (green=exit 0 판정).
- 갭 요약: <아직 못 닫은 것 1~3개, ⚠️로 표시한 항목의 압축 재진술>.
