> **BLUF:** 에이전트 정의(.claude/agents/*.md)의 줄 예산 폼 — 뼈대 없이 예산만 강제한다.

<!--
폼 대상: .claude/agents/*.md (에이전트 정의 = LLM에 주입되는 프롬프트)
예산(둘 다 상한): 110줄 · 산문 한 줄 80자·한 문장.
  표 행·코드펜스 내부는 줄 길이에서 면제한다 — 쪼개면 깨진다.
  YAML frontmatter(name·description·tools·model)는 길이·문장에서 면제하되
  줄 수에는 센다 — 저자가 못 고르는 건 값의 줄바꿈이지 필드 개수가 아니다.
  이 수치의 정본은 이 파일이고 ai-harness check-doc가 파싱해 쓴다.
  다른 곳에 옮겨 적지 말 것 — 골격을 두 군데 두면 어긋난다.
BLUF 상한은 안 건다 — 에이전트 정의는 `> **BLUF:**` 줄을 쓰지 않는다.
  안 쓰는 관례에 상한을 박으면 다음 저자가 그 상한의 존재도 모른 채 어긴다.
왜 별도 폼인가: docs/docs-format/agents.md는 AGENTS.md·CLAUDE.md의 폼이다 —
  이름만 겹치는 별개 문서지 에이전트 정의의 폼이 아니다. 유형 폼이 없으면
  rules 예산으로 떨어지는데, 그건 근거가 아니라 우연이다.
왜 rules와 같은 수치가 아닌가: 수치가 같아도 근거가 다르면 같이 움직여선
  안 된다. rules의 상한은 조문 하나짜리 문서 크기에서 왔고 여기 110은
  페르소나 실측에서 왔다 — 붙어 있던 두 수치를 갈라놓는 것이 이 폼의 목적이다.
왜 산문 규칙(80자·한 문장)은 유지하나: 프롬프트라도 사람이 읽고 고친다.
  AGENTS.md·CLAUDE.md도 매 세션 주입되는 문서지만 같은 규칙을 진다.
정직 표기: 110은 이상적 수치가 아니라 실측에서 왔다(실사용 실측: 가장 큰
  정의가 95줄이고 거기에 한 절 분량만 얹은 값이다). 헤드룸을 5로 잡으면
  가장 큰 파일의 다음 편집이 곧바로 막혀 예산이 아니라 동결이 된다.
넘었을 때: 예산부터 올리지 마라. 먼저 절을 내려라 — 여러 페르소나가 같은
  문구를 이고 있으면 그건 규칙 문서로 내리고 여기선 조항 번호로 가리킨다.
뼈대는 없다 — 페르소나마다 담당 차원·판정 기준이 다르다. 예산만 강제한다.
-->
