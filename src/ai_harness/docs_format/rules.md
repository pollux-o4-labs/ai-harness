<!--
폼 대상: docs/rules/<NN>-<slug>.md
예산(셋 다 상한): 100줄 · 산문 한 줄 80자·한 문장 · BLUF 한 줄 100자(마커 제외).
  표 행·코드펜스 내부는 줄 길이에서 면제한다 — 쪼개면 깨진다.
  자동생성 블록(BLUF-INDEX)도 면제한다 — 저자가 손댈 수 없는 몫을 예산에
  넣으면 지킬 방법이 없다. 줄이려면 원본 문서의 BLUF를 쳐라(그쪽이 정본).
  이 수치의 정본은 이 파일이고 ai-harness check-doc가 파싱해 쓴다.
  다른 곳에 옮겨 적지 말 것 — 골격을 두 군데 두면 어긋난다.
서사(왜 이 규칙이 필요했나)는 조문 문서에 쓰지 않는다. 근거는 docs/history/로
  내리고 "## 관련"에서 링크한다(재서술 금지).
아래 뼈대를 복붙하고 <> 안을 채운 뒤, 이 HTML 주석과 힌트 주석은 지운다.
-->

# 규칙 <NN> — <제목>

> **BLUF:** <이 문서가 무엇에 관한 사항인지 한 줄. 근거·수치·처방은 담지 않는다.>

## 규칙

<!-- 제1조, 제2조... 조문 형식. 의무 동사로 쓴다("~해야 한다" / "~해서는
     아니 된다"), "~하는 게 좋다" 같은 권고형 금지. 조마다 "다만," 단서로
     예외를 명시할 수 있다. -->

### 제1조 (<조 제목>)

<조문 본문>

## 강제 수단 (정직 표기)

<!-- 이 규칙을 실제로 막는 기계 게이트가 있는지 사실대로 적는다. 없으면
     "없다"고 쓰고 무엇에 의존하는지(예: 커밋 전 적대검증의 리뷰어 판정)
     적는다. 이 절은 예외 조항이 아니라 현재 강제 수준의 사실 표기다. -->

## 관련

<!-- 근거가 된 docs/history/B-*.md 링크(제정 근거), 관련 규칙·ADR·스킬 링크. -->
