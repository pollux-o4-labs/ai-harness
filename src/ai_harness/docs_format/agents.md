> **BLUF:** AGENTS.md·CLAUDE.md의 줄 예산 폼 — 뼈대 없이 예산만 강제한다.

<!--
폼 대상: AGENTS.md · CLAUDE.md (루트 및 하위 저장소 주입본)
예산(셋 다 상한): 160줄 · 산문 한 줄 80자·한 문장 · BLUF 100자.
  표 행·코드펜스 내부는 줄 길이에서 면제한다 — 쪼개면 깨진다.
  이 수치의 정본은 이 파일이고 scripts/check_doc_form.py가 파싱해 쓴다.
  다른 곳에 옮겨 적지 말 것 — 골격을 두 군데 두면 어긋난다.
왜 상한이 있나: 이 파일은 매 세션 자동 로드된다. 여기 있는 모든 줄이 모든
  세션의 컨텍스트 비용이라, 안 읽히는 문서와 예산이 같을 수 없다.
왜 rules의 100이 아닌가: rules는 조문 하나짜리 문서다. 이 파일은 라우터
  (어디를 볼지)와 이 파일만이 정본인 규범(스택·감독모델·설계원칙)을 함께
  진다 — 다른 문서들이 이 파일을 정본으로 지목하고 있어 그 절들은 못 내린다.
정직 표기: 160은 이상적 수치가 아니다. 재작성으로 줄일 수 있는 데까지 줄인
  뒤의 실측 크기에서 왔다. 즉 "여기가 옳은 크기다"가 아니라 "지금보다 더
  커지지 마라"는 뜻이다. 더 내릴 방법이 나오면 이 수치를 내려라.
넘었을 때: 예산부터 올리지 마라. 먼저 절을 내려라 — 내릴 수 있는지는 "이
  절을 정본으로 지목하는 다른 문서가 있나"로 판정한다. 있으면 못 내린다
  (그 링크들을 같이 고치는 것이 진짜 비용이다). 없으면 정본을 만들어 내리고
  여기엔 링크만 남긴다.
뼈대는 없다 — 저장소마다 내용이 다르다. 예산만 강제한다.
-->
